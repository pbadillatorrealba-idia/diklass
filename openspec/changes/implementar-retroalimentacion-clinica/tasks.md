# Tasks: Retroalimentación clínica

Convención: Rojo-Verde-Refactor obligatorio (Constitución II) — cada tarea de prueba se observa
fallando por la razón prevista antes de la implementación que cubre. Las suites pgTap y de
integración viva se ejecutan en el job `database` de CI y su ciclo local se observa en el clúster
PostgreSQL scratch con shims en `/tmp/verify-005/` (patrón del quickstart de 002; nunca
`supabase start`). `quickstart.md` del cambio se crea en 1.1 y se va llenando de forma incremental
(consolidación final en 5.2). Cada tarea indica su criterio observable de terminación y su
trazabilidad a los requisitos de [la spec](specs/retroalimentacion-clinica/spec.md); las decisiones
Dn remiten a [design.md](design.md).

## 1. Base de datos: garantías de la spec (pruebas primero)

- [ ] 1.1 Escribir `supabase/tests/012_retroalimentacion_clinica.sql` (pgTap, patrón de `008_registro_clinico.sql`) en rojo y crear `quickstart.md` con su primera evidencia: acción FR-063 `clinical_feedback_recorded` al registrar y `corrective_record_created` al corregir, atribución a la identidad autenticada de quien registra aunque sea distinta del veterinario que atendió (FR-063 · FR-070 · SC-049 · US10-AC13), validación server-side del contenido — vocabularios de D2 con `parcial`/`desconocida` aceptados y valores fuera de vocabulario o claves desconocidas rechazados (FR-040 · US10-AC8, FR-043 · SC-023 · US10-AC9) —, registro solo sobre consulta cerrada y rechazo sobre consulta abierta (D3), tratamiento vacío aceptado sin impedir la evolución (FR-057 · US10-AC12), inmutabilidad del registro ante `UPDATE` (`CLINICAL_FEEDBACK_IMMUTABLE`) con corrección como registro nuevo y original recuperable junto con correctivas sucesivas (FR-024 · SC-022 · US10-AC5), e epicrisis y diagnóstico original byte-idénticos tras registrar retroalimentación con cambio de diagnóstico (FR-024 · SC-022 · US10-AC3/AC4). Verificación: rojo observado por `/tmp/verify-005/` y en el job `database` de CI (URL registrada en `quickstart.md`), fallando exactamente los asserts que cubre la migración de 1.2.
- [ ] 1.2 Escribir `supabase/migrations/012_retroalimentacion_clinica.sql` (D4, D5, D6): solo funciones `returns trigger` y sus triggers — `validate_clinical_feedback` (forma cerrada del contenido de D2, vocabularios exactos, `consultationId` que resuelve con cast defensivo a consulta `closed` de la misma clínica; `CLINICAL_FEEDBACK_INVALID_CONTENT`, SQLSTATE 23514) y `guard_clinical_feedback_immutable` (todo `UPDATE` de `clinical_feedback` rechazado; `CLINICAL_FEEDBACK_IMMUTABLE`, SQLSTATE 23514) — sin ninguna función callable nueva. Verificación: suite 011 verde en `/tmp/verify-005/` y en CI, suites 001–008 verdes con 012 aplicada, y `supabase gen types` sin diff sobre `src/lib/supabase/database.types.ts` (D6).
- [ ] 1.3 Confirmar que el mapping `clinical_record_action` no necesita cambios — `INSERT` de `clinical_feedback` ya emite `clinical_feedback_recorded`, `status='corrective'` ya emite `corrective_record_created`, y su rama `UPDATE` de `clinical_feedback` queda sin uso práctico porque D4 prohíbe todo `UPDATE` — y documentarlo en `quickstart.md`. Verificación: nota presente y suite 011 verde.

## 2. Modelos y lecturas puras (pruebas de unidad primero)

- [ ] 2.1 Escribir en rojo `tests/unit/retroalimentacion/schema.test.ts`: esquemas Zod y tipos de D2 con los siete contenidos de FR-018 con campo propio (tabla de cobertura de D2: `treatmentApplied`, `adherence`, `evolution`+`evolutionNote`, `adverseEvents`, `revisedDiagnosis`, `treatmentModification`), vocabularios con `parcial`/`desconocida` sin respuestas binarias (FR-040 · US10-AC8), tratamiento vacío y modificación anulables (FR-057 · US10-AC12), `treatmentApplied` y `treatmentModification` distinguibles entre sí (FR-018 · US10-AC10), `revisedDiagnosis` como cambio posterior sin tocar el diagnóstico original (FR-024 · US10-AC4) y eventos adversos estructurados con severidad y descripción (FR-041). Verificación: `bun test tests/unit/retroalimentacion` en rojo por el esquema inexistente; luego verde.
- [ ] 2.2 Escribir en rojo `tests/unit/retroalimentacion/feedback-summary.test.ts`: `buildFeedbackTimeline` con cronología estable sin sobrescritura (FR-056 · US10-AC11) y corrección marcada con el original recuperable y correctivas sucesivas resueltas al original (FR-024 · US10-AC5), `collectAdverseEvents` diferenciado del resto de la evolución con `grave` destacable (FR-041 · SC-035 · US10-AC2), `aggregateFeedback` con recuentos por categoría sin interpretar texto libre (FR-043 · SC-023 · US10-AC9) y `buildFeedbackAntecedents` con la evolución previa como antecedente presentable (FR-042 · US10-AC6), con umbral ≤ 300 ms sobre 100 entradas (presupuesto de `design.md`). Verificación: `bun test tests/unit/retroalimentacion` en rojo por funciones inexistentes; luego verde.

## 3. Servicios de dominio (unidad local + integración viva en CI)

- [ ] 3.1 Escribir en rojo `tests/unit/retroalimentacion/feedback-service.test.ts` y `tests/integration/retroalimentacion/feedback.test.ts` (patrón fake-client de `clinical-mutations.test.ts` y `describe.skipIf(!isLiveSupabase)` de `live-supabase.ts`) y luego implementar `createFeedbackEntry` y `correctFeedbackEntry` de `src/features/retroalimentacion/feedback-service.ts`: registro sobre consulta cerrada asociado de forma estructurada (FR-018 · US10-AC1) con fecha de registro distinguible de la de la consulta (FR-039 · US10-AC7), tratamiento vacío aceptado (FR-057 · US10-AC12), tratamiento aplicado y su modificación almacenados distintos (FR-018 · US10-AC10), adherencia parcial/desconocida sin forzar binaria (FR-040 · US10-AC8), rechazo sobre consulta abierta (D3), corrección como registro nuevo vía `createCorrectiveRecord` con el original y las correctivas previas recuperables y atribuidas a su autor (FR-024 · SC-022 · US10-AC5), epicrisis y diagnóstico idénticos tras registrar — comparación antes/después (FR-024 · SC-022 · US10-AC3/AC4) —, atribución al segundo veterinario con evento `clinical_feedback_recorded` (FR-063 · FR-070 · SC-049 · US10-AC13) y varias entradas conservadas sin sobrescribirse (FR-056 · US10-AC11); escrituras solo vía `src/lib/attribution` (contrato de atribución). Verificación: `bun test tests/unit/retroalimentacion` rojo→verde localmente y `tests/integration/retroalimentacion` verde en CI (`SUPABASE_LIVE_TESTS=1`) con aserciones de tiempo ≤ 2 s por operación (presupuestos de `design.md`).
- [ ] 3.2 Escribir en rojo las pruebas de lectura de `tests/unit/retroalimentacion/feedback-service.test.ts` y `tests/integration/retroalimentacion/feedback.test.ts` y luego implementar `listFeedbackByConsultation` y `listFeedbackByPatient` de `feedback-service.ts`: recuperación agregada de los categóricos de adherencia y evolución de varias consultas sin texto libre (FR-043 · SC-023 · US10-AC9), eventos adversos recuperables de forma diferenciada (FR-041 · SC-035 · US10-AC2), fechas de registro y consulta distinguibles en la lectura (FR-039 · US10-AC7) y evolución previa expuesta por `buildFeedbackAntecedents` para el resumen de seguimiento futuro (FR-042 · US10-AC6, requisito de integración de D9 con `buildFollowUpSummary` documentado en `design.md` y `quickstart.md`). Verificación: ídem 3.1, con aserción de tiempo ≤ 2 s para el panel completo (≤ 5 consultas, ≤ 30 entradas).

## 4. Interfaz (WCAG 2.2 AA; sin Playwright en esta rama)

- [ ] 4.1 Implementar `src/components/retroalimentacion/`: `feedback-form.tsx` (TanStack Form + Zod con los controles de selección de `adherence`, `evolution` y `severity` — SC-037: sin texto libre para los campos categóricos —, `adverseEvents` con `grave` destacable y descriptores de texto libre opcionales), `feedback-timeline.tsx` (cronología sin sobrescritura con `AttributionBadge` y `CorrectionHistory` reutilizados: FR-056 · US10-AC11, FR-024 · US10-AC5, FR-070) y `adverse-event-report.tsx` (eventos adversos diferenciados y con `grave` destacado: FR-041 · SC-035 · US10-AC2). Etiquetas programáticas, operabilidad por teclado, foco visible y contraste del paletín vigente (WCAG 2.2 AA), `testID` estables. Verificación: `bun run typecheck` y Biome sobre los archivos de este cambio verdes, más pruebas unitarias de la lógica del formulario (validación D2 y vocabularios de FR-040) en verde; verificación visual y axe declaradas pendientes (D11).
- [ ] 4.2 Implementar las rutas `src/app/(protected)/follow-up/index.tsx` (selector de paciente reutilizando `listPatients`/`getPatient` importados) y `src/app/(protected)/follow-up/[patientId].tsx` (panel de evolución del seguimiento: selector de consulta cerrada con lo indicado en su epicrisis efectiva como contexto, formulario de nueva entrada, cronología con correcciones, reporte diferenciado de eventos adversos y evolución previa presentada como antecedente — FR-042 · US10-AC6 en esta superficie —). Montaje mínimo documentado en `quickstart.md`, incluida la entrada de navegación pendiente desde `/home` (requisito de integración, D9–D10). Verificación: ídem 4.1 más el flujo registro/corrección cubierto por las pruebas de servicio de 3.1/3.2 sobre esta superficie.

## 5. Compuertas y evidencia

- [ ] 5.1 Ejecutar las compuertas locales disponibles (`bun run typecheck`, `bunx biome check --write` sobre los archivos de este cambio, `bun test tests/unit/retroalimentacion tests/integration/retroalimentacion`) y dejar el job completo de CI en verde; registrar los comandos, resultados y URLs de las ejecuciones en `quickstart.md`. Verificación: lista de comandos y resultados + URLs de CI registradas, con el rojo previo de 1.1 enlazado.
- [ ] 5.2 Consolidar `quickstart.md` del cambio con la evidencia real acumulada desde 1.1 (ciclo rojo→verde de la suite 011 en `/tmp/verify-005/` y CI, unidades rojo→verde, integración viva) y los pendientes explícitos: verificación visual local, compuerta axe + teclado/foco/viewport sobre `/follow-up` (requisito de integración sobre `tests/e2e/web/accessibility.spec.ts`, D11), integración de FR-042 en `summaries.ts` y `/consultations/[id]` (requisito de integración de D9; SC-036 parcial hasta entonces), aceptación humana de SC-036 y SC-037 (tiempo < 2 min) y ratificación de los vocabularios clínicos de D2 (Open Questions). Verificación: documento completo y sin afirmar como aceptado nada que no lo esté.

## 6. Revisión y cierre

- [ ] 6.1 Generar el reporte de revisión en español con `requesting-code-review` (subagente) sobre el rango completo de la rama y dejarlo publicado en la PR que cree el orquestador, o adjunto al reporte final si la PR aún no existe (esta rama nunca crea PRs). Verificación: reporte existente y su ubicación registrada.
- [ ] 6.2 Aplicar los hallazgos con `receiving-code-review` (verificación técnica por ítem, respuestas en los hilos de la PR cuando exista), commit + push de los cambios y comentario del resultado. Verificación: ítems respondidos, commits pusheados y resultado comentado.
- [ ] 6.3 Si la implementación desvía `design.md` o estas tareas, actualizar los artefactos con `openspec-update-change` antes del cierre. Verificación: artefactos coherentes entre sí.

## 7. Revisión de la PR #28 (2026-09-24)

Hallazgos de `/code-review` publicados en la PR (10 comentarios en línea sobre `24209f7`). Antes de
corregir se integró `main` (PR #29 y #30: migraciones 010 y 011). pgTap, la integración viva y
Playwright corren en local contra el Supabase compartido (podman), así que cada rojo se observó
antes del arreglo.

- [x] 7.1 Integrar `main` sin conflictos y renombrar la suite pgTap a `012_retroalimentacion_clinica.sql` para no compartir prefijo con `011_captura_voz_revision.sql` (D6). Verificación: `supabase db reset` con 001–012, `supabase test db` verde en las 14 suites y `bun run db:types` sin diff.
- [x] 7.2 Exigir en el servidor el vínculo de la corrección: `status = 'corrective'` ⇔ `supersedes_event_id`, y ese evento es el `clinical_feedback_recorded` de un original de la misma clínica y consulta (FR-024 · SC-022 · US10-AC5 · SC-023, D7; hallazgo 1). Verificación: asserts 31–35 de la suite 012 rojos sin el arreglo («no exception») y verdes con él.
- [x] 7.3 Exigir el `consultationId` en forma canónica (SC-023 · SC-035, D5; hallazgo 2). Verificación: asserts 36–37 (mayúsculas, llaves) rojo→verde.
- [x] 7.4 Reporte de eventos adversos con las versiones vigentes y las sustituidas a demanda, sin resalte (FR-041 · SC-035 · US10-AC2, D10; hallazgo 3). Verificación: e2e «el reporte de eventos adversos muestra una vez…», rojo 3 ítems → verde 1.
- [x] 7.5 «Corregir entrada» solo en la versión vigente (FR-024 · US10-AC5, D7/D10; hallazgo 4). Verificación: e2e «solo la versión vigente…», rojo 2 botones → verde 1.
- [x] 7.6 Cerrojo síncrono contra el doble envío en `/follow-up/[patientId]` (FR-056 · SC-023, D4/D10; hallazgo 5). Verificación: e2e «dos pulsaciones rápidas…», rojo 2 filas → verde 1.
- [x] 7.7 Antecedentes con etiquetas del vocabulario y texto vacío acorde a lo listado (FR-042 · US10-AC6, D10; hallazgo 6). Verificación: e2e «los antecedentes muestran el vocabulario en español» rojo→verde.
- [x] 7.8 Sesión expirada en las lecturas del panel: `setAccessState('expired')` + diálogo; el aviso de carga se deriva del error vigente (US11 · Constitución IV, D10; hallazgo 7). Verificación: e2e «una lectura rechazada por sesión expirada…» rojo (sin diálogo) → verde.
- [x] 7.9 `listFeedbackByConsultations(ids)` sobre las consultas ya cargadas por el panel, sin repetir `listConsultationsByPatient` (FR-043 · SC-023, D8; hallazgo 8). Verificación: prueba unitaria nueva (sin lectura de consultas) y suite viva verde.
- [x] 7.10 Evento de registro del original leído por `(entity_type, entity_id, action)` con `limit 1` (D7; hallazgo 9). Verificación: prueba unitaria de los filtros rojo→verde.
- [x] 7.11 Reutilizar `compararFilas` de `src/features/registro/summaries.ts` (D8; hallazgo 10). Verificación: suites unitarias de cronología verdes sin cambios.
- [x] 7.12 Actualizar `design.md` (D5, D7, D8, D9, D10, D11 y riesgos), este archivo y `quickstart.md`. Verificación: `openspec validate implementar-retroalimentacion-clinica` válido.
- [ ] 7.13 Compuerta axe WCAG 2.2 AA + teclado/foco/viewport sobre `/follow-up` en `tests/e2e/web/accessibility.spec.ts`. Pendiente declarado (D11): la revisión añadió Playwright funcional, no la compuerta de accesibilidad.
